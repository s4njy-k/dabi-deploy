"""DABI unified ingest package.

Each pipeline is a subcommand invoked via:
    python -m dabi_ingest <pipeline> [--opts]

Subcommands dispatch through dabi_ingest.__main__.
"""
__version__ = "0.1.0"
