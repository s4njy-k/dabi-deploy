# Pinned for reproducibility — verified to pull prebuilt wheels on python:3.14-slim ARM64

# Core
opensearch-py==2.6.0
clickhouse-driver==0.2.10
clickhouse-connect==0.8.10
redis==5.2.0
requests==2.32.3
aiohttp==3.13.5
google-cloud-secret-manager==2.20.2
google-cloud-storage==2.18.2

# Data (pandas/pyarrow deferred until a pipeline requires them — no stable py3.14 ARM64 wheel yet)
duckdb==1.5.3

# Resilience
tenacity==9.0.0

# Observability
structlog==24.4.0
